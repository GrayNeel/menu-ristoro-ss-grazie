# TODO before the feast

Two things in `index.html` are best-guess placeholders carried over from the
design handoff (see `chats/chat1.md`) and need real content:

1. **Dish descriptions & allergen lists** — every `.item-detail` block (set
   menu courses, à la carte items). Marked with `<!-- TODO -->` comments in
   the HTML. Confirm with the kitchen/volunteers before printing or
   publishing.
2. **18–20 Settembre programme times** — the three `.program-row` entries in
   "La festa" section. Currently placeholder times (opening, feast day,
   procession). Marked with a `<!-- TODO -->` comment in the HTML.
